export type RuntimeContext = {
  repo: "polymarket-whale-copy-bot";
  family: "polymarket";
  market: string;
  signal: string;
  dryRun: boolean;
  orderSize: string;
  privateKeyPreview: string;
};
